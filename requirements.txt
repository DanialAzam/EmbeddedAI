# Full PC-side requirements (training + optimization + inference).
# Installed by install.bat / install.sh
ultralytics>=8.2.0
torch>=2.2.0
torchvision>=0.17.0
torch-pruning>=1.3.0
onnx>=1.15.0
onnxruntime>=1.17.0
onnxslim>=0.1.31
opencv-python>=4.9
numpy>=1.24
pyyaml>=6.0
matplotlib>=3.8
pandas>=2.0
openpyxl>=3.1   # Excel results export (export_results.py)
psutil>=5.9     # live CPU/RAM monitor (GUI status bar) + benchmark resource sampling
tqdm
# optional: only needed for the UA-DETRAC upgrade path in download.py
roboflow>=1.1.0
